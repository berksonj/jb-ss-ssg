{
  "name": "jb-ss-ssg",
  "version": "1.0.0",
  "description": "JB's Super Simple Static Site Generator.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js && node scripts/watch.js",
    "serve": "npx http-server -c-1 ./dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "md5": "^2.3.0",
    "nunjucks": "^3.2.4",
    "sanitize.css": "^13.0.0",
    "sass": "^1.70.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
